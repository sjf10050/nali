version: "2"

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - gosec
    - nilerr

formatters:
  enable:
    - gofmt
    - goimports
